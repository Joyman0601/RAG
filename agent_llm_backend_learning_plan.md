# 初级后端转 Agent 大模型应用开发学习路线

目标：尽可能压缩学习时间，以通过大模型应用开发 / Agent 应用开发相关岗位面试为主。深度算法、模型训练、复杂论文复现可以入职后再补。

## 你的定位

不要把自己包装成模型算法工程师，而是包装成：

> 熟悉后端系统，能基于 LLM、RAG、Tool Calling 和 Agent 工作流落地企业级大模型应用的后端开发。

你的优势是后端工程能力，包括接口设计、数据库、权限、日志、部署、稳定性和业务系统集成。

## 总体学习周期

建议周期：4 到 6 周。

如果每天能投入 4 小时：

1. 3 天：跑通主流 LLM API。
2. 7 天：完成 RAG 知识库项目。
3. 7 天：加入 Tool Calling / Function Calling。
4. 7 天：改造成 Agent 工作流。
5. 5 天：补评估、日志、权限、成本控制。
6. 5 天：整理项目 README、架构图、面试话术。

## 第 1 阶段：LLM API 基础

时间：3 到 5 天。

重点掌握：

- Chat / Responses API 的基本调用方式。
- system / developer / user message 的职责。
- token、上下文窗口、temperature、top_p。
- 流式输出。
- JSON 输出和结构化输出。
- 超时、重试、降级。
- Prompt 注入、幻觉、上下文污染。

必须能讲清楚：

- 为什么不能只靠 prompt 保证输出格式？
- 为什么要用 JSON Schema 或 structured outputs？
- 模型输出不稳定时，后端如何兜底？
- token 成本如何估算和控制？

练习任务：

- 写一个简单的后端接口 `/chat`。
- 支持流式返回。
- 支持结构化 JSON 输出。
- 增加超时和错误重试。

## 第 2 阶段：RAG 知识库问答

时间：7 到 10 天。

这是面试最核心的部分。

重点掌握：

- 文档解析：PDF、Markdown、网页、数据库文本。
- 文档切分：按标题、段落、token 切分。
- embedding 的作用。
- 向量数据库的基本使用。
- top_k、score threshold、metadata filter。
- rerank 的作用。
- 引用来源返回。
- 权限过滤。
- 增量更新。

必须能讲清楚：

- RAG 和微调有什么区别？
- chunk size 怎么选？
- 为什么向量检索会召回错误？
- 如何减少幻觉？
- 如何保证回答基于原文？
- 如何处理用户没有权限访问的文档？

推荐项目：

> 企业知识库问答系统：上传文档，自动切分入库，用户提问后检索相关片段，模型基于片段回答，并返回引用来源。

最低功能：

- 文档上传。
- 文档切分。
- embedding 入库。
- 向量检索。
- 基于检索结果回答。
- 返回引用来源。
- 记录问答日志。

加分功能：

- metadata 过滤。
- 用户权限过滤。
- rerank。
- 检索结果可视化。
- 简单评估集。

推荐技术：

- 后端：FastAPI / Spring Boot / NestJS，选择你最熟的。
- 数据库：PostgreSQL。
- 向量库：pgvector / Qdrant / Milvus / Elasticsearch。
- 缓存：Redis。
- 模型：OpenAI / Claude / 通义千问 / DeepSeek / 智谱等。

## 第 3 阶段：Tool Calling / Function Calling

时间：7 天。

这是从普通聊天机器人到 Agent 应用的分水岭。

重点掌握：

- 模型如何决定调用工具。
- 工具参数 schema 如何设计。
- 工具执行失败如何重试。
- 工具调用如何做权限控制。
- 工具调用如何做审计日志。
- 查询类工具和写操作工具要分开。
- 高风险操作要人工确认。
- 工具结果如何再次喂给模型。

必须能讲清楚：

- Function calling 是模型真的执行函数吗？
- 工具调用失败怎么办？
- 怎么防止模型越权调用工具？
- 为什么写操作工具要加确认机制？
- 工具参数为什么要强校验？

练习项目：

> 业务操作 Agent：让 Agent 能调用业务工具，比如查询订单、创建工单、查库存、发送邮件。

最低功能：

- 查询订单工具。
- 查询用户工具。
- 创建工单工具。
- 工具参数校验。
- 工具调用日志。
- 失败重试。

加分功能：

- 写操作人工确认。
- 用户权限校验。
- 工具执行超时控制。
- 敏感字段脱敏。
- 幂等性设计。

## 第 4 阶段：Agent 工作流

时间：7 天。

重点掌握：

- 单 Agent 和多 Agent 的区别。
- planning / routing / reflection 的作用和局限。
- Agent 状态管理。
- session memory 和长期 memory。
- handoff：不同 Agent 之间转交任务。
- guardrails：输入校验、输出校验、敏感操作拦截。
- workflow 比完全自由的 Agent 更适合生产环境。

必须能讲清楚：

- Agent 和普通 Chatbot 的区别？
- 多 Agent 一定比单 Agent 好吗？
- 为什么生产环境不能让 Agent 完全自由行动？
- Agent 的状态应该存在哪里？
- 如何限制 Agent 的行为边界？

推荐框架：

- OpenAI Agents SDK：理解 tools、handoffs、guardrails、tracing。
- LangGraph：理解图式工作流、节点、状态、条件路由。

项目改造：

把前面的业务操作 Agent 改成工作流：

1. 用户提出问题。
2. 意图识别。
3. 路由到知识库问答、订单查询或工单创建。
4. 调用对应工具。
5. 对高风险操作进行确认。
6. 返回结果并记录日志。

## 第 5 阶段：评估、监控和上线

时间：5 到 7 天。

很多初级候选人只会 demo，不会工程化。你把这块讲清楚，会明显加分。

重点掌握：

- 离线评估集。
- 检索命中率。
- 答案忠实度。
- 格式正确率。
- 工具调用成功率。
- 日志和 tracing。
- 成本控制。
- 限流、重试、熔断。
- 数据安全和权限控制。

必须能讲清楚：

- 如何判断 RAG 系统效果好不好？
- 如何排查一次错误回答？
- 如何降低 token 成本？
- 如何记录 Agent 执行过程？
- 如何做线上灰度？

建议准备一个 30 到 100 条的评估集：

- 用户问题。
- 标准答案。
- 期望引用文档。
- 期望调用工具。
- 实际回答。
- 是否通过。

## 第 6 阶段：MCP 基础

时间：2 到 3 天。

MCP 不需要一开始深挖，但面试可能会问。

你需要知道：

- MCP 是一种让 Agent 连接外部工具和数据源的标准协议。
- 核心概念包括 tools、resources、prompts。
- 它适合把公司内部系统封装成统一工具接口。
- 风险包括工具权限、prompt injection、数据泄露、工具描述投毒。

必须能讲清楚：

- MCP 解决什么问题？
- MCP 和普通 HTTP API 有什么区别？
- 如果把内部订单系统接入 MCP，要注意什么安全问题？

## 最终必须准备的 2 个项目

### 项目 1：RAG 企业知识库助手

功能：

- 文档上传。
- 文档解析。
- chunk 切分。
- embedding。
- 向量检索。
- metadata 过滤。
- 基于上下文回答。
- 返回引用来源。
- 问答日志。
- 简单评估集。

面试讲法：

> 这个项目主要解决企业内部知识检索和问答问题。我没有直接让模型凭空回答，而是先根据用户问题召回相关文档片段，再把片段作为上下文交给模型生成答案，并返回引用来源。为了减少幻觉，我做了检索阈值、引用来源、无答案兜底和日志追踪。

### 项目 2：业务操作 Agent

功能：

- 用户意图识别。
- 工具调用。
- 查询订单。
- 查询用户。
- 创建工单。
- 权限校验。
- 参数校验。
- 高风险操作确认。
- 工具调用日志。
- 失败重试。

面试讲法：

> 这个项目不是简单聊天机器人，而是一个能和业务系统交互的 Agent。模型负责理解用户意图和生成工具参数，后端负责工具执行、权限校验、参数校验、超时控制和日志审计。写操作不会直接执行，会先让用户确认，避免模型误操作。

## 高频面试题

### RAG 相关

- RAG 和微调有什么区别？
- RAG 的完整流程是什么？
- chunk size 怎么选？
- top_k 怎么选？
- 召回结果不准怎么办？
- 为什么需要 rerank？
- 怎么减少幻觉？
- 怎么返回引用来源？
- 用户权限如何和 RAG 结合？
- 文档更新后向量库如何同步？

### Tool Calling 相关

- Function calling 是模型真的执行函数吗？
- 工具 schema 怎么设计？
- 工具参数错误怎么办？
- 工具调用失败怎么办？
- 如何防止模型越权调用工具？
- 查询工具和写操作工具有什么区别？
- 为什么高风险操作要人工确认？

### Agent 相关

- Agent 和 Chatbot 的区别是什么？
- 多 Agent 一定更好吗？
- Agent memory 怎么设计？
- Agent 状态应该放在哪里？
- 如何限制 Agent 的行动边界？
- 什么情况下不应该使用 Agent？
- workflow 和自由 Agent 怎么取舍？

### 工程化相关

- 如何做 LLM 应用评估？
- 如何记录一次完整 Agent 执行链路？
- 如何降低 token 成本？
- 如何做限流和熔断？
- 如何处理模型接口超时？
- 如何做数据脱敏？
- 如何防 prompt injection？

## 暂时不要深挖的内容

为了压缩时间，下面这些可以放到入职后再学：

- Transformer 数学细节。
- 从零训练大模型。
- LoRA / QLoRA 深度调参。
- CUDA 和分布式训练。
- 复杂论文复现。
- 多模态复杂应用。
- AutoGPT 类无限自主 Agent。

## 简历关键词

可以在简历中使用这些关键词：

- LLM 应用开发
- RAG
- Embedding
- Vector Database
- pgvector / Milvus / Qdrant
- Tool Calling
- Function Calling
- Agent Workflow
- LangChain
- LangGraph
- OpenAI Agents SDK
- MCP
- Prompt Injection 防护
- LLM Evaluation
- Tracing
- Token 成本优化

## 面试中的核心表达

你要反复强调这几个观点：

1. LLM 不是业务系统本身，后端要负责边界、权限、校验和执行。
2. RAG 的核心不是简单向量检索，而是文档处理、召回质量、上下文构造和答案评估。
3. Agent 不是让模型随便行动，而是把模型放进可控工作流里。
4. 生产环境要关注日志、评估、成本、安全和降级。
5. 工具调用必须有参数校验、权限控制、超时、重试和审计。

## 最短冲刺安排

如果只有 2 周：

第 1 到 2 天：

- 跑通 LLM API。
- 学会流式输出和 JSON 输出。

第 3 到 6 天：

- 做 RAG 知识库。
- 实现上传、切分、embedding、检索、回答、引用来源。

第 7 到 10 天：

- 加 Tool Calling。
- 实现订单查询、用户查询、创建工单。

第 11 到 12 天：

- 加权限、日志、错误处理、人工确认。

第 13 到 14 天：

- 整理 README、架构图、面试题答案。

两周版本不追求功能多，要追求能讲清楚架构、取舍和工程边界。

